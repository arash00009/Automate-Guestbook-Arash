apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: arash-nginx-demo
  labels:
    app: arash-nginx-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: arash-nginx-demo
